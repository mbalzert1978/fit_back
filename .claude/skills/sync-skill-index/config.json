{
  "skills_dir": ".claude/skills",
  "claude_md": ".claude/skills/CLAUDE.md",
  "tree_root_label": ".claude/skills/",
  "begin_marker": "<!-- BEGIN GENERATED: sync-skill-index -->",
  "end_marker": "<!-- END GENERATED: sync-skill-index -->",
  "buckets": [
    { "name": "Utility", "tagline": "one small reusable thing, the same way every time" },
    { "name": "Verification", "tagline": "checks the quality of a final output" },
    { "name": "Data enrichment", "tagline": "pulls external knowledge in" },
    { "name": "Orchestration", "tagline": "chains other skills or steps into a multi-step playbook" }
  ],
  "skills": {
    "build-verifier": { "bucket": "Utility", "summary": "scaffolds a new Verification-bucket skill from a description; fills a bundled `assets/` template." },
    "clc2-codec": { "bucket": "Utility", "summary": "canonical codec for the CLC2 coaching-ledger format (compress / decompress / validate)." },
    "handoff": { "bucket": "Utility", "summary": "compacts the current conversation into a handoff document for another agent." },
    "issue-status": { "bucket": "Utility", "summary": "scans docs/issues/ for frontmatter status fields and prints a grouped status table (blocked / open / closed / …) to chat — read-only, no files written." },
    "issue-close": { "bucket": "Utility", "summary": "closes one issue — sets status: closed in its frontmatter and appends a completion note to the issue file's own body, no central progress file involved." },
    "run-tests": { "bucket": "Utility", "summary": "runs the project's test suite via the one canonical invocation (plain dotnet test from the repo root) and surfaces the pass/fail result." },
    "apply-validator-findings": { "bucket": "Utility", "summary": "takes the findings already produced by a set of quality validators and fixes every one, applying each finding's own located remedy — never re-reviewing or inventing new scope." },
    "fixer-alternative-classes-with-different-interfaces": { "bucket": "Utility", "summary": "aligns two classes doing the same job under different method names/signatures — Rename Method/Extract Superclass — paired with `verifier-alternative-classes-with-different-interfaces`." },
    "fixer-comments": { "bucket": "Utility", "summary": "replaces a WHAT-comment with a name that carries the meaning (Extract Method/Rename/Introduce Assertion), leaving genuine WHY comments untouched — paired with `verifier-comments`." },
    "fixer-data-class": { "bucket": "Utility", "summary": "moves behavior into an anemic Domain type (Move Method/Encapsulate Field/Collection) — paired with `verifier-data-class`." },
    "fixer-data-clumps": { "bucket": "Utility", "summary": "extracts a recurring group of variables/parameters into its own Value Object/class (Extract Class + Introduce Parameter Object) — paired with `verifier-data-clumps`." },
    "fixer-dead-code": { "bucket": "Utility", "summary": "deletes a variable/parameter/field/method/class with zero remaining references — paired with `verifier-dead-code`." },
    "fixer-divergent-change": { "bucket": "Utility", "summary": "splits a class that changes for many unrelated reasons into one class per concern (Extract Class/Superclass/Subclass) — paired with `verifier-divergent-change`." },
    "fixer-duplicate-code": { "bucket": "Utility", "summary": "replaces near-identical copy-pasted fragments with one shared implementation (Extract Method/Class, Substitute Algorithm) — paired with `verifier-duplicate-code`." },
    "fixer-feature-envy": { "bucket": "Utility", "summary": "moves a method to the class whose data it actually operates on (Move Method) — paired with `verifier-feature-envy`." },
    "fixer-inappropriate-intimacy": { "bucket": "Utility", "summary": "separates two classes reaching into each other's internals (Hide Delegate/Move Method/Change Bidirectional Association) — paired with `verifier-inappropriate-intimacy`." },
    "fixer-incomplete-library-class": { "bucket": "Utility", "summary": "centralizes a scattered library/framework-gap workaround (Introduce Foreign Method/Local Extension) — paired with `verifier-incomplete-library-class`." },
    "fixer-large-class": { "bucket": "Utility", "summary": "splits a class whose fields/methods cluster into more than one natural group (Extract Class/Subclass/Interface) — paired with `verifier-large-class`." },
    "fixer-lazy-class": { "bucket": "Utility", "summary": "inlines or collapses a class that no longer earns its own existence (Inline Class/Collapse Hierarchy) — paired with `verifier-lazy-class`." },
    "fixer-long-method": { "bucket": "Utility", "summary": "extracts cohesive pieces out of a method mixing several responsibilities/abstraction levels (Extract Method, Decompose Conditional, Replace Method with Method Object) — paired with `verifier-long-method`." },
    "fixer-long-parameter-list": { "bucket": "Utility", "summary": "collapses excess parameters into an object or a derivable lookup (Introduce Parameter Object/Preserve Whole Object) — paired with `verifier-long-parameter-list`." },
    "fixer-message-chains": { "bucket": "Utility", "summary": "hides a navigation chain behind a method on the object the client already holds (Hide Delegate) — paired with `verifier-message-chains`." },
    "fixer-middle-man": { "bucket": "Utility", "summary": "removes a class whose methods do nothing but forward to another class (Remove Middle Man), preserving intentional port/adapter seams — paired with `verifier-middle-man`." },
    "fixer-parallel-inheritance-hierarchies": { "bucket": "Utility", "summary": "merges two hierarchies extended in lockstep into one (Move Method/Field) — paired with `verifier-parallel-inheritance-hierarchies`." },
    "fixer-primitive-obsession": { "bucket": "Utility", "summary": "replaces a raw primitive standing in for a domain concept with a Value Object/typed class (Replace Data Value with Object, Replace Type Code) — paired with `verifier-primitive-obsession`." },
    "fixer-refused-bequest": { "bucket": "Utility", "summary": "replaces an ill-fitting inheritance relationship with delegation or a narrower base (Replace Inheritance with Delegation, Extract Superclass) — paired with `verifier-refused-bequest`." },
    "fixer-shotgun-surgery": { "bucket": "Utility", "summary": "consolidates one conceptual change scattered across many files into a single place (Move Method/Field, Inline Class) — paired with `verifier-shotgun-surgery`." },
    "fixer-speculative-generality": { "bucket": "Utility", "summary": "removes an unused just-in-case abstraction (Inline Method/Class, Collapse Hierarchy) — paired with `verifier-speculative-generality`." },
    "fixer-switch-statements": { "bucket": "Utility", "summary": "moves a discriminant switched on in several places onto the type/closed DU's cases (Replace Type Code with Subclasses/State-Strategy) — paired with `verifier-switch-statements`." },
    "fixer-temporary-field": { "bucket": "Utility", "summary": "extracts a field only meaningful during one algorithm into its own object (Extract Class, Replace Method with Method Object) — paired with `verifier-temporary-field`." },
    "photo-interpreter": { "bucket": "Utility", "summary": "reads photos/screenshots and extracts visible data as Markdown." },
    "refine-prompt": { "bucket": "Utility", "summary": "improves/hardens a draft prompt or task template and returns the rewritten text + a changelog — never executing what the prompt asks." },
    "reflect": { "bucket": "Utility", "summary": "end-of-session experience extraction; persists structured experience files with decay metadata." },
    "skill-linker": { "bucket": "Utility", "summary": "syncs skill symlinks from a central source directory into target directories; reads `config.json`." },
    "strategic-compact": { "bucket": "Utility", "summary": "suggests manual context compaction at logical intervals to preserve context." },
    "sync-skill-index": { "bucket": "Utility", "summary": "regenerates the skill-inventory region of CLAUDE.md from the filesystem + a bucket map." },
    "to-prd": { "bucket": "Utility", "summary": "converts current conversation context into a PRD and publishes it to the issue tracker." },
    "verbessere-text": { "bucket": "Utility", "summary": "improves a short text or keywords into three tone variants (AskUserQuestion pick, source language kept), normalized dash-free by a bundled `scripts/strip_dashes.py`." },
    "compress-prompt": { "bucket": "Utility", "summary": "rewrites a prompt or text to use fewer tokens while preserving intent, applying a fixed set of token-reduction techniques and reporting a before/after token estimate." },
    "worktree-erstellen": { "bucket": "Utility", "summary": "creates a git worktree under `.claude/worktrees/` and makes the local-only context (`.claude/`, `CLAUDE.md`, `CONTEXT.md`) available inside it — platform-appropriate symlink/junction/hardlink, idempotent." },
    "worktree-entfernen": { "bucket": "Utility", "summary": "tears down a worktree only when it is final in main (branch merged) and its tree is clean — junction-safe removal, deletes the merged branch; `--force` to override." },

    "architecture-adr-check": { "bucket": "Verification", "summary": "checks code changes against this repo's architecture-decision docs and the issue they implement (paths via `config.json`, missing config = `CONFIG ERROR` not a silent pass) — invariants honored, acceptance criteria met — returning PASS/FAIL plus a `Findings: <n>` count." },
    "coach-payload-verify": { "bucket": "Verification", "summary": "verifies a committed coaching payload is well-formed and internally consistent." },

    "verifier-alternative-classes-with-different-interfaces": { "bucket": "Verification", "summary": "reviews a diff for two classes performing the same job under different method names/signatures, blocking interchangeable use. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-comments": { "bucket": "Verification", "summary": "reviews a diff for comments explaining WHAT unclear code does instead of a genuinely non-obvious WHY. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-data-class": { "bucket": "Verification", "summary": "reviews a diff for anemic classes holding only fields/getters/setters with no behavior of their own. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-data-clumps": { "bucket": "Verification", "summary": "reviews a diff for the same small group of variables/parameters recurring together across signatures or fields instead of being extracted into a class. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-dead-code": { "bucket": "Verification", "summary": "reviews a diff for variables/parameters/fields/methods/classes with zero remaining references. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-divergent-change": { "bucket": "Verification", "summary": "reviews a diff for a single class edited for many unrelated reasons in one change. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-duplicate-code": { "bucket": "Verification", "summary": "reviews a diff for two or more near-identical code fragments that should be shared instead of copy-pasted. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-feature-envy": { "bucket": "Verification", "summary": "reviews a diff for a method that accesses another object's data more than its own. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-inappropriate-intimacy": { "bucket": "Verification", "summary": "reviews a diff for two classes reaching into each other's internal fields/methods. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-incomplete-library-class": { "bucket": "Verification", "summary": "reviews a diff for ad-hoc workarounds scattered around a library/framework gap instead of centralizing them. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-large-class": { "bucket": "Verification", "summary": "reviews a diff for a class that accumulated too many fields/methods/unrelated responsibilities. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-lazy-class": { "bucket": "Verification", "summary": "reviews a diff for a class that no longer does enough to justify its own existence. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-long-method": { "bucket": "Verification", "summary": "reviews a diff for methods mixing several responsibilities/abstraction levels in one body. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-long-parameter-list": { "bucket": "Verification", "summary": "reviews a diff for methods/constructors taking more than three or four parameters. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-message-chains": { "bucket": "Verification", "summary": "reviews a diff for navigation chains (`a.B().C().D()`) coupling a client to the whole intermediate object structure. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-middle-man": { "bucket": "Verification", "summary": "reviews a diff for a class whose methods do nothing but delegate to another class. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-parallel-inheritance-hierarchies": { "bucket": "Verification", "summary": "reviews a diff for two class hierarchies that must be extended in lockstep. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-primitive-obsession": { "bucket": "Verification", "summary": "reviews a diff for raw primitives standing in for a concept with its own rules instead of a small dedicated type. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-refused-bequest": { "bucket": "Verification", "summary": "reviews a diff for a subclass that only uses some of its parent's inherited members. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-shotgun-surgery": { "bucket": "Verification", "summary": "reviews a diff for one conceptual change requiring small edits scattered across many classes/files. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-speculative-generality": { "bucket": "Verification", "summary": "reviews a diff for abstractions built \"just in case\" for an imagined future need with no current caller. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-switch-statements": { "bucket": "Verification", "summary": "reviews a diff for the same discriminant switched/if-chained on in more than one place instead of using polymorphism/a closed sum type. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "verifier-temporary-field": { "bucket": "Verification", "summary": "reviews a diff for a field only meaningfully set during one algorithm/call and empty otherwise. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "design-pattern-fit-check": { "bucket": "Verification", "summary": "reviews a diff for shapeless/procedural code where a named GoF/architectural pattern (Strategy, State, Factory, Builder, Decorator, Observer, Command, Chain of Responsibility, Template Method, Adapter, Repository) would express it more cleanly — requires naming the concrete pattern and sketching the seams, not just \"add an abstraction\" — returning BLOCK/APPROVE plus a `Findings: <n>` count." },
    "docs-code-consistency": { "bucket": "Verification", "summary": "checks docs (docstrings, README, the configured decision docs, CONTEXT.md) against the current code and returns PASS/FAIL plus an itemized, located drift report — checker, not fixer." },
    "illegal-state-check": { "bucket": "Verification", "summary": "reviews a diff's data/type model for representable-but-invalid states — a flag/enum plus optional fields only valid in some combinations, an unconstrained primitive standing in for a constrained domain, a mutable object with violable invariants — and pushes to make the bad state unrepresentable, not merely checked. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "language-idiom-check": { "bucket": "Verification", "summary": "reviews a diff for missed idioms of the language actually in use (pattern matching, records, collection pipelines, destructuring, ...) and for imperative code that should be declarative (pattern matching over cascades, immutable values, pure functions). Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "lint-and-format-check": { "bucket": "Verification", "summary": "runs this repo's configured `linter`/`formatter` (command + args from `config.json`, any language) — unconfigured proposes a pair by detected language and stops as `CONFIG ERROR` rather than guessing — returning PASS/FAIL plus a `Findings: <n>` count." },
    "qa-check": { "bucket": "Verification", "summary": "runs the test suite via `run-tests`, and — toggleable via `config.json` — flags changed production units whose configured test location didn't change alongside them; the unit→test-location map is an ordered rule list in `config.json`, never assumed. Slice *shape* is `slice-shape-check`'s job, not this one's." },
    "slice-shape-check": { "bucket": "Verification", "summary": "checks the mechanically-verifiable half of this repo's feature-slice form — every use-case package carries its Test-API and fakes, and no spec reaches past the Test-API into domain/handler/mappers/fakes/infrastructure. Structure only, no code judgment; missing config = `CONFIG ERROR` not a silent pass. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "structure-placement-check": { "bucket": "Verification", "summary": "flags changed test files living outside the configured test-root prefixes — a purely mechanical file-*path* check, no code content read; missing config = `CONFIG ERROR` not a silent pass. The cheap, objective first gate before any content-level review. Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "propose-skills": { "bucket": "Verification", "summary": "scans chat history for repeated manual tasks that should be skills and proposes each (name, bucket, one job), skipping what existing skills cover." },
    "review-against-rules": { "bucket": "Verification", "summary": "reviews a diff/branch against this repo's configured coding-standard dirs and, if configured, a reference implementation, via the `senior-code-reviewer` subagent — required config missing = `CONFIG ERROR` not a silent pass — returning BLOCK/APPROVE plus a `Findings: <n>` count." },
    "skill-audit": { "bucket": "Verification", "summary": "flags existing skills that straddle more than one bucket and recommends a split or trim." },
    "skill-tune-up": { "bucket": "Verification", "summary": "audits skills against the five structural levers; flags mechanical weaknesses." },
    "solid-principles-check": { "bucket": "Verification", "summary": "reviews a diff against the five SOLID principles (SRP/OCP/LSP/ISP/DIP), each with its own smell checklist, plus a mechanical file-size pre-check (oversized files are a classic SRP smell). Returns BLOCK/APPROVE plus a `Findings: <n>` count." },
    "thermo-nuclear-code-quality-review": { "bucket": "Verification", "summary": "extremely strict, language-agnostic, deliberately open-ended maintainability review — abstraction quality, spaghetti conditions, canonical-layer reuse, orchestration atomicity. Standalone/manual use only (\"tear this PR apart\") — its open-ended bar has no fixed point, so it is not part of `validate-fix-loop`'s default validator set; see `solid-principles-check`/`design-pattern-fit-check`/`language-idiom-check`/`illegal-state-check` for the bounded, loop-safe split of its rubric." },
    "token-budget-audit": { "bucket": "Verification", "summary": "audits persistent-context artifacts (CLAUDE.md, SKILL.md files, memory) against token-budget heuristics and returns PASS/FAIL plus a located, itemized report." },
    "verifier-audit": { "bucket": "Verification", "summary": "flags skills that should become verifiers or gain an objective Pass/Fail or grade-out-of-10 check." },
    "verify-issue-breakdown": { "bucket": "Verification", "summary": "verifies that a `to-issues` breakdown is a sound tracer-bullet decomposition before publish." },

    "fetch-cv": { "bucket": "Data enrichment", "summary": "reads one CV / Lebenslauf from a local PDF (Read, page mode), guards against unusable content (missing path / empty / encrypted / no text layer), and extracts the recognised fields + language — the single source `anschreiben` references instead of restating the read logic." },
    "fetch-job-posting": { "bucket": "Data enrichment", "summary": "loads one job posting from a URL (WebFetch) or file (Read), guards against unusable content, and extracts the recognised fields + language — the single source `anschreiben` and `interview-notes` reference instead of restating the fetch logic." },
    "graphify-windows": { "bucket": "Data enrichment", "summary": "builds and queries a persistent knowledge graph from a codebase — god nodes, community detection, query/path/explain tools; Windows-compatible variant." },
    "kotlin-coroutines-flows": { "bucket": "Data enrichment", "summary": "Kotlin Coroutines and Flow patterns (structured concurrency, StateFlow, testing)." },
    "kotlin-exposed-patterns": { "bucket": "Data enrichment", "summary": "JetBrains Exposed ORM patterns (DSL, DAO, transactions, HikariCP)." },
    "kotlin-ktor-patterns": { "bucket": "Data enrichment", "summary": "Ktor server patterns (routing, plugins, auth, Koin, kotlinx.serialization)." },
    "kotlin-patterns": { "bucket": "Data enrichment", "summary": "idiomatic Kotlin patterns and best practices." },
    "kotlin-testing": { "bucket": "Data enrichment", "summary": "Kotlin testing with Kotest, MockK, coroutine testing, Kover." },

    "anschreiben": { "bucket": "Orchestration", "summary": "drives a strict state-machine pipeline (job posting → fact/direction review → draft → finalize) that fills a LaTeX template and writes `Cover_<firma>.tex`; delegates posting loading to `fetch-job-posting`." },
    "audit-to-issues": { "bucket": "Orchestration", "summary": "audits a repo on a chosen axis (over-engineering / illegal-states / pythonic) and turns each finding into a clean, reviewed, deduped issue — delegates to `ponytail-audit` and `to-issues`." },
    "auto-commit": { "bucket": "Orchestration", "summary": "groups a working tree's changes into cohesive Conventional-Commits, previews the plan, then commits each group." },
    "coaching": { "bucket": "Orchestration", "summary": "adaptive personal coaching session driven by a structured JSON coaching payload." },
    "done": { "bucket": "Orchestration", "summary": "end-of-session wrapper: runs `/reflect`, prints summary, updates task state." },
    "interview-notes": { "bucket": "Orchestration", "summary": "produces interview-prep notes for a specific interview, grounded in a fetched/read job posting and the candidate's CV — company/role summary, tailored questions with CV-grounded talking points, honest gap framing, questions to ask, optional salary and STAR stories." },
    "issues-to-prs": { "bucket": "Orchestration", "summary": "implements each selected open issue as an independent, parallel git-worktree agent and ships exactly one PR per issue — disjointness-gated, no shared merge." },
    "prototype": { "bucket": "Orchestration", "summary": "builds a throwaway prototype to flesh out a design; routes between two branches." },
    "tdd": { "bucket": "Orchestration", "summary": "test-driven development with red-green-refactor loop." },
    "to-issues": { "bucket": "Orchestration", "summary": "breaks a plan/PRD into tracer-bullet issues, gates with `verify-issue-breakdown` before publish." },
    "validate-fix-loop": { "bucket": "Orchestration", "summary": "dispatches the quality validators listed in `config.json` (default: 8 general validators plus the 23 `verifier-<smell>` code-smell checks) in parallel each iteration, then routes each validator's findings — via `config.json`'s `fixer_map` — to its own `fixer-<smell>` skill where one is mapped, or batches unmapped validators' findings into one `apply-validator-findings` call, running fixer dispatches sequentially; repeats until every validator reports zero findings, total findings plateau across an iteration, or `max_iterations` is hit." },
    "multi-agent-thermo-nuclear-review": { "bucket": "Orchestration", "summary": "manual-only multi-agent fan-out of `thermo-nuclear-code-quality-review` — per configured lens, diverse finders + one adversarial verifier run in parallel, reconciled against the agent's own reading; delegates the rubric and size-pass to thermo-nuclear by path." }
  }
}
